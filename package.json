{
  "name": "after-effects-ruler-animator",
  "version": "0.1.0",
  "private": true,
  "description": "Dockable After Effects ScriptUI panel for animated ruler rigs.",
  "scripts": {
    "install:ae": "python3 scripts/install-after-effects.py",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
